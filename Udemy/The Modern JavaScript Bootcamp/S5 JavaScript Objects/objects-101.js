The Modern JavaScript Bootcamp


// person object name ,age , location 

//declare and define the person object 

let person ={
    name:'mariam',
    age:26,
    location:'Tanta'
}

console.log(`${person.name} is ${person.age} and live in ${person.location}`)
person.age++
console.log(`${person.name} is ${person.age} and live in ${person.location}`)
